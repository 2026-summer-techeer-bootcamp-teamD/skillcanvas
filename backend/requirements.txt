fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
alembic>=1.13
psycopg[binary]>=3.2
pydantic-settings>=2.5
python-dotenv>=1.0
anthropic>=0.40   # Claude API SDK (백엔드 AI 기능 3장)
clerk-backend-api>=6,<7   # Clerk 세션 JWT 검증 + 유저 조회. 코드가 6.x API에 의존하므로 메이저 고정
prometheus-fastapi-instrumentator>=7.0   # /metrics 노출 (Prometheus 연동)
prometheus-client>=0.20   # ask_claude() AI 호출 계측용 Counter/Histogram (llm.py)

# dev (포매터/린터/테스트)
black>=24.0
ruff>=0.6
pytest>=8.0
httpx>=0.27   # FastAPI TestClient가 사용
