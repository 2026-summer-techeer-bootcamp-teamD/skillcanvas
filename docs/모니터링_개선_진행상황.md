> 기준: `docs/DASHBOARD_IMPROVEMENT (1).md`(2026-07-16, Notion export) 개선 계획 실행 트랙.
> `docs/진행상황.md`(계측 전반 로그)와 별도로, 이 문서는 **Grafana 대시보드/패널 구성 작업**만 추적한다.

## [완료] Overview 대시보드 신설 (2026-07-16)

- `monitoring/grafana/provisioning/dashboards/json/skillcanvas-overview.json` 신규 추가
- 구성: 상단 Stat 6개(RPS / p95 응답 지연 / 5xx 에러율 / DB 커넥션 수 / AI 호출률 / AI 실패율) → **API**(기본 펼침) → **AI**(접힘) → **Infra(Postgres)**(접힘) → **Business**(접힘) row
- 각 row 상세 패널에 해당 세부 대시보드로 가는 drill-down 링크(`${__url_time_range}`로 시간 범위 유지) 포함
- 모든 stat/합계 패널에 `or vector(0)` 적용해 "No data" 방지(계획서 2단계 원칙)
- 임시 Grafana/Prometheus 컨테이너로 프로비저닝·PromQL 문법·row 접힘 구조 검증 완료(사용자 실행 중인 컨테이너는 미접촉)

### 의도적으로 뺀 것
- CPU/메모리 패널: `node-exporter`/`cAdvisor`가 아직 없어서 제외(계획서 1-6, 아래 "남은 일" 참고). 계측 추가 전에 넣으면 항상 "No data"만 뜨는 상태가 되어 오히려 계획서가 지적한 문제("만들다 만 대시보드로 보임")를 재현하게 됨

## [드롭] AI 토큰·비용 계측 (계획서 1-1, 구현 2026-07-16 → 드롭 2026-07-17)

- 2026-07-16에 `backend/app/core/llm.py`에 `ai_tokens_total{model,kind}`/`ai_cost_usd_total{model}` 계측과 `skillcanvas-ai-calls.json` 비용 패널 6개(구간 총 비용/시간당 비용/호출당 평균 비용/캐시 적중률/모델별 비용 비중/토큰 종류별 사용량), Overview 상단 4번 슬롯("AI 시간당 비용")까지 구현했었음
- 사용자 판단(2026-07-16 당시): "쓸지 안 쓸지 모르겠다" → 커밋 확정 전 `git stash`로 임시 보류(태그 `grafana-overview-monitoring-wip-20260717`)
- 2026-07-17: 우선순위표 작업 재개하며 확정 판단 — **드롭하고 미착수로 되돌림**
  - `backend/app/core/llm.py`, `skillcanvas-ai-calls.json`을 커밋 상태(HEAD)로 되돌림
  - Overview 상단 4번 슬롯은 "AI 시간당 비용" 대신 원래 의도였던 **"DB 커넥션 수"**(`sum(pg_stat_database_numbackends) or vector(0)`)로 복원
  - stash 자체는 `grafana-overview-monitoring-wip-20260717` 태그로 계속 보관 — 나중에 재도입하려면 `git stash list --format='%H %gs'`로 SHA 확인 후 `git stash apply <sha>`
- 계획서 1-1/1-2(토큰·비용 계측, `feature` 라벨)는 이 드롭으로 다시 **미착수** 상태

---

## 남은 일 (`DASHBOARD_IMPROVEMENT (1).md` 우선순위 기준)

| 순위 | 항목 | 상태 |
| --- | --- | --- |
| 1 | Postgres 대시보드 임포트 (ID `9628`) | 미착수 |
| 2 | 2단계 "No data" → 0 처리 | **부분 완료** — Overview 신규 패널만 적용. `skillcanvas-ai-calls.json`(전체 7개 패널), `skillcanvas-5xx-by-handler.json`, `skillcanvas-business-metrics.json`은 아직 미적용 |
| 3 | node-exporter + cAdvisor 추가, Node Exporter Full(ID `1860`) 임포트 | 미착수 |
| 4 | `feature` 라벨 추가 (1-2) | 미착수 |
| 5 | Overview 신설 | **완료** |
| 6 | Template variable(`$feature`/`$model`)·Annotation(배포 시각) | 미착수 |
| 7 | 히스토그램 버킷 조정(1-3), heatmap 패널 | 미착수 |
| 1-1 | AI 토큰·비용 계측 | **드롭됨(2026-07-17)** — 위 섹션 참고, stash로 보관 중 |

### 계획서엔 없던 추가 항목 (7/16 할 일 참고 이미지에서 발견, 위 1~7 다음 순서로)

| 순위 | 항목 | 상태 | 출처 |
| --- | --- | --- | --- |
| 8 | 엔드포인트별 RPS/p95 "그래프+Mean/Max 테이블" 콤보 패널 | 미착수 | 참고 이미지1 (다른 팀 대시보드) |
| 9 | DB 커넥션 풀 사용률 게이지 (Busy/Max) — `backend/app/core/db.py`의 SQLAlchemy `engine.pool`에 계측 추가 필요 | 미착수 | 참고 이미지2의 "Tomcat Threads Busy/Max"를 우리 스택(SQLAlchemy)으로 대응시킨 항목 |

> ⚠️ 대시보드 JSON은 로컬에서 확인 후 커밋해야 서버(EC2)에 반영됨 — `DASHBOARD_IMPROVEMENT (1).md` 4단계 함정 ② 참고. Overview 대시보드(`skillcanvas-overview.json`)는 아직 커밋 전 상태(로컬 검증만 완료).
